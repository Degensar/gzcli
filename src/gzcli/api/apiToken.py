"""API wrappers for the API-token endpoints.

The GZ::CTF release this client targets exposes no api-token endpoints, so there
is nothing to wrap here yet. The ApiToken / ApiTokenCreateModel / ApiTokenResponse
data models are kept in ``gzcli.api.models`` for forward compatibility.
"""
